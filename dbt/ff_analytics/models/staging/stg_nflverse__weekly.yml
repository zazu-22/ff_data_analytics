version: 2

models:
  - name: stg_nflverse__weekly
    description: "Staged weekly player stats from raw nflverse weekly Parquet."
    columns:
      - name: season
        tests: [not_null]
      - name: week
        tests: [not_null]
      - name: gsis_id
        tests: [not_null]
