version: 2

models:
  - name: stg_nflverse__players
    description: "Staged player reference from raw nflverse players Parquet."
    columns:
      - name: gsis_id
        description: "Primary key from nflverse player reference."
        tests:
          - not_null
          - unique
